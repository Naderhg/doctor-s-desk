services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: doctors_desk
    ports:
      - "5432:5432"
    volumes:
      - doctors_desk_pg:/var/lib/postgresql/data

volumes:
  doctors_desk_pg:
